coverage:
  status:
    project:
      default:
        # basic
        target: auto
        threshold: 0%

ignore:
  - dapr/proto                  # - Generated GRPC client
  - tests                       # - tests (includes tests/ext/<name>/ for all extensions)
  - .venv                       # - environment
